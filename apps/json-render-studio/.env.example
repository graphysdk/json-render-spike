# Copy to `.env`. Falls back to apps/agents-api/.env when this file is absent.
# Optional: without a key the studio generates through your Claude subscription, via Claude Code.
ANTHROPIC_API_KEY=

# Optional overrides.
# JSON_RENDER_STUDIO_MODEL=claude-sonnet-5
# JSON_RENDER_STUDIO_PORT=4320
